import Slider from "./Slider";
import MainContainer from "./MainContainer";

const Body = () => {
  return (
    <div className="flex">
      <Slider />
      <MainContainer />
    </div>
  );
};

export default Body;
